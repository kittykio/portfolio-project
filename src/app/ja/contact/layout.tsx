import { staticMetadata } from '@/lib/seo';
export const metadata = staticMetadata('/contact', 'ja');
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
