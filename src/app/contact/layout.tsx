import { staticMetadata } from '@/lib/seo';
export const metadata = staticMetadata('/contact', 'en');
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
