'use client';
import { useEffect, useMemo, useState } from 'react';
import { useRouter } from 'next/navigation';
import { useLocale } from '@/components/LocaleContext';
import { getLocalePath } from '@/i18n/config';

export default function CommandPalette() { const router = useRouter(); const { locale } = useLocale(); const [open, setOpen] = useState(false); const [query, setQuery] = useState(''); const [content, setContent] = useState<Array<{label:string;href:string;kind:string}>>([]); const actions = useMemo(() => [['Home', '/'], ['Projects', '/projects'], ['Blog', '/blog'], ['Now', '/now'], ['Saved', '/saved'], ['Résumé', '/resume'], ['Contact / Hire me', '/contact']].map(([label, href]) => ({ label, href: getLocalePath(href, locale), kind: 'Page' })), [locale]); useEffect(() => { if (open && !content.length) void fetch(`/api/search?locale=${locale}`).then((response) => response.json()).then((data) => setContent([...data.projects, ...data.posts])).catch(() => undefined); }, [open, locale, content.length]); const results = [...actions, ...content].filter((item) => item.label.toLowerCase().includes(query.toLowerCase())).slice(0, 12); useEffect(() => { const handler = (event: KeyboardEvent) => { if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === 'k') { event.preventDefault(); setOpen((value) => !value); } if (event.key === 'Escape') setOpen(false); }; window.addEventListener('keydown', handler); return () => window.removeEventListener('keydown', handler); }, []); if (!open) return <button type="button" onClick={() => setOpen(true)} className="rounded-full border border-border px-2 py-1 text-[10px] font-bodyBold hover:border-flame-500 hover:text-flame-500" aria-label="Open command palette">⌘K</button>; return <div className="fixed inset-0 z-[70] flex items-start justify-center bg-surface-inverse/40 p-4 pt-28" onMouseDown={() => setOpen(false)}><div className="w-full max-w-xl rounded-3xl bg-canvas p-4 shadow-2xl" onMouseDown={(event) => event.stopPropagation()}><input autoFocus value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search projects, posts, and pages…" className="w-full rounded-2xl border border-border bg-canvas px-4 py-3 outline-none focus:border-flame-500" /><div className="mt-3 space-y-1">{results.map((item) => <button key={item.href} onClick={() => { router.push(item.href); setOpen(false); }} className="flex w-full items-center justify-between rounded-xl px-4 py-3 text-left hover:bg-surface-muted"><span>{item.label}</span><span className="text-xs text-gray-500">{item.kind}</span></button>)}{!results.length && <p className="p-4 text-sm text-gray-500">No matching item.</p>}</div></div></div>; }
